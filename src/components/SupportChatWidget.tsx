import React from 'react';

// This component is now just a placeholder since we're using the Voicera AI widget directly in HTML
const SupportChatWidget: React.FC = () => {
  // The Voicera AI widget is now loaded directly in the HTML
  // No React component needed - the widget handles everything
  return null;
};


export default SupportChatWidget;
